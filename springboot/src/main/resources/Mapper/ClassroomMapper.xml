<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybiats.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.days.linker.mapper.ClassroomMapper">
    <select id="findAll" resultType="com.days.linker.entity.Classroom">
        SELECT * FROM classroom
    </select>
    <select id="findClassroom" resultType="com.days.linker.entity.Room">
        SELECT Room FROM classroom
    </select>
</mapper>